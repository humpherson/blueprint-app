/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "", // Remove any subdirectory, leave this empty
  assetPrefix: "", // Ensure assets are loaded correctly from the root
};

export default nextConfig;
